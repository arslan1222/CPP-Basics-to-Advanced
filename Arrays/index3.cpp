#include<iostream>
using namespace std;

int main(){                                            // Calculate the sum of all the elements in the given array;
    // int array[]={3,4,10,11};
    // int size = sizeof(array)/sizeof(array[0]);      

    // int ele=0;
    // for(int i=0; i<size; i++){
    //     ele = array[i] + ele;
    // }
    // cout<<ele<<"\n";

                                                   // Find the maximum value out of all the element in the array;

    // int array[]={1,2,13,4,15};
    
    // int size = sizeof(array)/sizeof(array[0]);
    // int max =array[0];
    // for(int i=0; i<=size; i++){
        
    //     if(max<array[i]){
    //         max=array[i];
    //     }
    // } 
    // cout<<max<<endl;

                                                        /*S    Searh if given element is present in the array or not, It it is not present
                                                            the return -1 else return the index; */
    // int ele =7;
    // int ans = -1;                                        // return -1 if the searching element does not exist

    // int array[]={2,5,10,4,8,9,10,7};
    // int size = sizeof(array)/sizeof(array[0]);

    // for(int i=0; i<size; i++){
    //     if(ele==array[i]){
    //         ele = i;
    //         // ans=i;
    //         break;
    //     }
    // }
    // cout<<ele<<endl;
    // cout<<ans<<endl;
    


    
    return 0;

}